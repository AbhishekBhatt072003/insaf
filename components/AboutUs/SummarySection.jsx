import styles from '../../styles/components/AboutUs/SummarySection.module.css'

export default function SummarySection() {
    return (
        <div> className={`${styles.mainBox}`}
        </div>
    )
}